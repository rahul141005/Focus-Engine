/**
 * Focus Engine — Firebase Messaging Service Worker (compatibility stub)
 * FCM messaging is handled by the main service worker (sw.js).
 * This file exists at root solely for Firebase SDK auto-discovery.
 * Do NOT add onBackgroundMessage here — sw.js handles all push logic.
 */
